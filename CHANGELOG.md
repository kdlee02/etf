# 변경 이력

부트캠프 회차별 피드백과 그 대응을 기록한다. 커밋만 봐선 "어떤 지적이 반영됐는지"가
안 보인다는 지적을 받아 만들었다 — 지적 한 줄, 대응 한 줄, 커밋 해시로 추적한다.

미대응 항목은 지우지 않고 **미대응**으로 남긴다. 안 한 것도 이력이다.

---

## 3주차 피드백 대응 (2026-08-07)

### [3-1] 스트리밍 중 CRAG 폴백 전환 UX — 대응 완료
> "스트리밍 중에 CRAG 폴백이 발생하면 사용자 화면에 이 전환이 어떻게 보이는지 확인하기 어렵다.
> 답변이 절반쯤 나오다가 갑자기 출처가 바뀌면 사용자가 혼란스러울 수 있다."

**재현됨 — 실제 버그였다.** `ask_stream`이 폴백할 때 같은 `on_token`을 `_run_web`에 그대로
넘겨서, 버려질 scoped 미리보기 뒤에 web 답변이 이어붙었다. 화면에 답변이 두 개로 보이다가
스트림 종료 후에야 교체됐다.

`STREAM_RESET` sentinel을 폴백 직전에 흘리고, UI가 버퍼를 비운 뒤 전환을 안내하도록 수정
(`_내부 자료에 근거가 없어 웹 검색으로 보완합니다…_`). `agent.py` · `app.py` ·
회귀 테스트 `test_ask_stream_crag_emits_reset_before_web_tokens`(emit 순서를 잠근다).

`_reground` 재작성은 티커 몇 개만 바뀌므로 신호를 보내지 않는다 — 확정 렌더에서 교체된다.

### [3-2] CRAG "근거 0건" 판정 기준 + 폴백 실패 시 최종 응답 — 대응 완료 (문서)
> "근거 0건 판정 기준이 무엇인지(도구 결과가 비었을 때인지, MIN_SCORE 컷오프인지, 둘 다인지)
> README만으로는 명확하지 않다. web 폴백도 실패하면 최종적으로 뭐라고 답하는지 문서화해달라."

코드는 이미 확정적이었고 문서만 없었다. README에 3개 섹션 추가:
- **근거 판정 3층** — 셋 다 아니고 그 상위인 `Answer.has_evidence`가 **툴 트레이스 전체**를
  본다. `search_concepts` 전용이 아니라, RAG가 빈손이어도 구조화 도구가 성공했으면 폴백하지 않는다.
- **폴백의 마지막 단계** — web도 실패하면(키 없음·네트워크 예외·결과 0건 전부 `found:False`로
  수렴) 재시도 없이 종료하며 사용자가 보는 최종 문구를 그대로 실었다.
- **알려진 구멍** — `GRADE_BYPASS=0.50` 위 구간은 2차 게이트를 건너뛰므로 유사도가 강한
  오탐은 어느 층도 못 거른다. 현재 코퍼스(정답 최고 0.42)에선 발생하지 않는다.

### [3-3] 라우터 분류 정확도 평가 부재 — 대응 완료
> "라우터 자체의 분류 정확도(예: reject로 잘못 보내 실제로는 답할 수 있는 질문을 거절하는 비율)에
> 대한 평가가 없다. eval_retrieval.py처럼 라우터 골드셋 평가도 추가하면 좋겠다."

`eval/eval_router.py` 신규. 25문항 골드셋(scoped 13 · web 5 · reject 7), 경계 케이스를
일부러 섞었다(투자 의도 말투인데 scoped, 도메인 안인데 코퍼스 밖이라 web).

지적받은 **false-reject를 1급 지표**로 출력한다. scoped↔web 오분류는 CRAG가 사후 보정하지만
false-reject는 사용자가 기능을 못 쓰고 떠나므로 같은 무게로 보지 않는다.

실측 (3회 반복, 75판정): **정확도 75/75 (100%) · false-reject 0% · false-accept 0% · 편차 0**

### [3-4] 2주차 지적 반영 여부가 안 보임 — 대응 완료
> "지난주 말씀드렸던 tools.py 문서 불일치와 MAX_TOOL_ROUNDS 실측 근거 부재가 이번 README에
> 명시적으로 언급되지 않아 반영 여부를 확인하기 어렵다."

이 파일(CHANGELOG.md)이 근본 대응이다. 개별 항목은 아래 2주차 절 참고.

### [3-5] (지적 밖) eval 케이스 stale — 대응 완료
3-3 작업 중 발견. `run_eval.py`의 `ac4_out_of_scope_web`이 "비트코인 살까?"에 `web_search`
호출을 기대하고 있었다. reject 라우트가 생긴 `9c71419` 이후 이 질문은 reject가 맞고
(README·`graph.py` 자체점검·라우터 골드셋 전부 reject), eval만 옛 기준으로 남아 라이브
스모크가 2/2 실패 중이었다. 두 케이스로 분리:
- `ac4_offdomain_reject` — "비트코인 살까?" → 도구 0회 + 거절
- `ac4b_domain_fact_web` — "미국 기준금리 몇 %야?" → `web_search` 호출

라이브 스모크 12/12 복구.

---

## 2주차 피드백 대응 (2026-07-30)

### [2-1] tools.py "Gemini에 노출하는" 문서 불일치 — 대응 완료
모델을 solar-pro3로 바꾼 뒤 주석에 Gemini 잔재가 남아 있었다. `5c583e5`에서 정리.
3주차에 잔존 2곳 추가 제거(`eval/run_eval.py` 독스트링, `tests/test_tools.py` 주석).
`tests/test_agent.py`의 "Gemini 버전에서 서로 덮어쓰던 버그"는 그 회귀 테스트가 왜 존재하는지의
기록이라 **의도적으로 유지**한다.

### [2-2] MAX_TOOL_ROUNDS 실측 근거 부재 — 대응 완료
`cfe5535`에서 `_run_scoped`에 라운드 수 INFO 로그를 붙였으나, 라이브러리가 로깅 핸들러를
설정하지 않아 **로그가 실제로는 안 보였다**. 3주차에 `run_eval.py`에서 `basicConfig`로 켜고
실측치를 README에 기록.

### [2-3] _reground 자가수정 1회 제한 — 미대응 (의도된 트레이드오프)
근거 없는 티커가 남는다는 건 도구 결과에 그 종목이 아예 없다는 뜻이라 재시도해도 모델이
끌어올 근거가 없다. 2회 이상은 지연·비용만 늘고 회수율은 미미하다는 판단으로 1회를 유지한다.
근거는 `agent.py:112-115` 주석에 있다. 3주차에 같은 판단을 CRAG 폴백에도 적용(웹 폴백 실패 시
재시도 없이 종료)하고 README에 명시했다.
